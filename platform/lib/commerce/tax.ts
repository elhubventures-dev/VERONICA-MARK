/**
 * Storefront tax policy: displayed product prices are tax-inclusive.
 * Do not add a separate tax line on top of catalog prices at checkout.
 */

export const PRICES_INCLUDE_TAX = true;

/** Short label beside product prices (cards, PDP, bag lines). */
export const TAX_INCLUSIVE_PRICE_HINT = "incl. tax";

/** Summary footnote on bag / checkout. */
export const TAX_INCLUSIVE_SUMMARY_NOTE = "All product prices include tax.";
